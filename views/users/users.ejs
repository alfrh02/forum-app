<!DOCTYPE html>
<html>
    <head>
        <meta charset UTF-8>
        <title><%=siteName%>: Users</title>
        <link rel="stylesheet" href="/style.css">
    </head>
    <body>
        <nav>
            <div class="container">
                <a href="/">Homepage</a>
            </div>
            <form method="POST" action="/searchresult">
                <input type="text" id="search" name="search"/>

                <label for="posts">posts</label>
                <input type="checkbox" id="posts" name="radio" value="posts"/>
                <label for="topics">topics</label>
                <input type="checkbox" id="topics" name="radio" value="topics"/>
                <label for="users">users</label>
                <input type="checkbox" id="users" name="radio" value="users"/>

                <input type="submit" value="Search"/>
            </form>
        </nav>
        <h3>Users</h3>
        <% for (let i = 0; i < result.length; i++) { %>
            <div class="index-item">
                <a href="/user/<%=result[i].userName%>"> <%=result[i].userName%></a>, user since <%=result[i].userCreationDate%>
            </div>
        <% } %>
    </body>
</html>