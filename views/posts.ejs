<!DOCTYPE html>
<html>
    <head>
        <meta charset UTF-8>
        <title><%=siteName%>: Posts</title>
    </head>
    <body>
        <a href="/">Homepage</a>
        <form method="POST" action="/searchresult/post">
            <input type="text" id="search" name="search"/>
            <input type="submit" value="Search"/>
        </form>
        <% for (let i = 0; i < result.length; i++) { %>
            <div>
                <a href="/post/<%=result[i].postId%>"> <%=result[i].name%>, <%=result[i].creationDate%></a>
            </div>
        <% } %>
    </body>
</html>